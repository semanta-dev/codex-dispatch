version: 2

builds:
  - id: codex-dispatch
    main: ./cmd/codex-dispatch
    binary: codex-dispatch
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "codex-dispatch_{{.Os}}-{{.Arch}}"
    # Windows ships as .zip (the platform convention) with a codex-dispatch.exe
    # inside; the launcher's Windows branch downloads and unzips it.
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [] # binary only

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot"

release:
  github:
    owner: semanta-dev
    name: codex-dispatch
  draft: false
  prerelease: auto
